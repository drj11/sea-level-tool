Fetch stuff from JASL, the Joint Archive for Sea Level

run:

    code/getinv # to fetch the inventory file
    code/dbinv.py # to convert it to sqlite
    code/getjasl.py # to fetch hourlies from one station
